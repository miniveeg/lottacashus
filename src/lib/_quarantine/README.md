# Quarantine

Files here are **not** on the product path.

- `local-case-battles.ts` — dual local settlement; Case Battles ships via `case-battle-v2` + RPCs only.

Do not re-import into `src/pages/CaseBattles/` or routes.
